services:
  - type: web
    name: web-story-deployment
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn webtruyen.wsgi:application"
